user              nginx;
worker_processes  1;

events {
    worker_connections  1024;
}

http {
    error_log   off;
    access_log  off;

    disable_symlinks off;
    autoindex on;

    server {
      listen 127.0.0.1:80;
      listen 127.0.0.1:81;
      root /usr/share/nginx/html/;
    }
}
